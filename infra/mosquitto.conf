# Local development only — anonymous access, no TLS.
# Production deployments must add per-device certificate auth per PRD Section 8.3
# before any real edge agent connects.
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
